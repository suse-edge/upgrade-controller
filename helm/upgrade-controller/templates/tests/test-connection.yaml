apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upgrade-controller.fullname" . }}-test-connection"
  labels:
    {{- include "upgrade-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upgrade-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
